specVersion: "v1"
serviceId: "zerowait-emergency"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "zerowait-emergency-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
